import * as React from "react";
import FuntionalCounter from "./FunctionalCounter";

const App = () => {
  return <FuntionalCounter initialCount={1} title={"My TS Counter"} />;
};

export default App;
