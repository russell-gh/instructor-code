import React from "react";
import "./Spinner.scss"; // Assuming you have saved your CSS in Spinner.css

const Spinner = () => {
  return <div className="loader"></div>;
};

export default Spinner;
